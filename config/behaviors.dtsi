/ {
    behaviors {
        caps_or_down: caps_or_down {
            compatible = "zmk,behavior-mod-morph";
            label = "caps_or_down";
            bindings = <&kp DOWN>, <&kp CAPSLOCK>;

            #binding-cells = <0>;
            mods = <(MOD_LSFT)>;
        };
    };
};